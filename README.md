# syncrpy-tui
Python Rsync TUI

The goal of this TUI is to create an easy to use browser/rsync tool.

keyboard keys:

q = quit

5 = copy

if ssh is activated 5 = rsync

9 = DELETE

m = make directory
